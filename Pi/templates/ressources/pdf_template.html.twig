<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Resource Details - {{ ressource.titre }}</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            line-height: 1.6;
            color: #333;
            padding: 20px;
        }
        .header {
            text-align: center;
            margin-bottom: 30px;
            padding-bottom: 20px;
            border-bottom: 2px solid #6C63FF;
        }
        .header h1 {
            color: #6C63FF;
            margin-bottom: 10px;
        }
        .section {
            margin-bottom: 20px;
            padding: 15px;
            background-color: #f8f9fa;
            border-radius: 5px;
        }
        .section-title {
            color: #6C63FF;
            margin-bottom: 10px;
            font-size: 18px;
            font-weight: bold;
        }
        .rating {
            text-align: center;
            margin: 20px 0;
            padding: 15px;
            background-color: #6C63FF;
            color: white;
            border-radius: 5px;
            font-size: 24px;
            font-weight: bold;
        }
        .info-item {
            margin-bottom: 10px;
        }
        .info-label {
            font-weight: bold;
            color: #6C63FF;
        }
        .footer {
            margin-top: 30px;
            text-align: center;
            font-size: 12px;
            color: #666;
        }
    </style>
</head>
<body>
    <div class="header">
        <h1>Resource Details</h1>
        <p>Generated on {{ "now"|date("F j, Y, g:i a") }}</p>
    </div>

    <div class="section">
        <div class="section-title">Basic Information</div>
        <div class="info-item">
            <span class="info-label">Title:</span> {{ ressource.titre }}
        </div>
        <div class="info-item">
            <span class="info-label">Type:</span> {{ ressource.type }}
        </div>
        <div class="info-item">
            <span class="info-label">Description:</span> {{ ressource.description }}
        </div>
        {% if ressource.lien %}
        <div class="info-item">
            <span class="info-label">Link:</span> {{ ressource.lien }}
        </div>
        {% endif %}
    </div>

    <div class="rating">
        Rating: {{ ressource.noteaverage }}/10
    </div>

    <div class="footer">
        <p>This document was automatically generated from the Resource Management System.</p>
    </div>
</body>
</html> 