<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Resource Details</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            line-height: 1.6;
            color: #333;
            margin: 20px;
        }
        .header {
            text-align: center;
            margin-bottom: 30px;
            border-bottom: 2px solid #4f46e5;
            padding-bottom: 10px;
        }
        .resource-title {
            color: #4f46e5;
            font-size: 24px;
            margin-bottom: 10px;
        }
        .resource-type {
            color: #6b7280;
            font-size: 16px;
        }
        .section {
            margin-bottom: 20px;
        }
        .section-title {
            color: #4f46e5;
            font-size: 18px;
            margin-bottom: 10px;
            border-bottom: 1px solid #e5e7eb;
            padding-bottom: 5px;
        }
        .description {
            text-align: justify;
        }
        .meta-info {
            margin-top: 30px;
            font-size: 14px;
            color: #6b7280;
        }
        .footer {
            margin-top: 50px;
            text-align: center;
            font-size: 12px;
            color: #6b7280;
            border-top: 1px solid #e5e7eb;
            padding-top: 10px;
        }
    </style>
</head>
<body>
    <div class="header">
        <h1 class="resource-title">{{ ressource.titre }}</h1>
        <div class="resource-type">{{ ressource.type }}</div>
    </div>

    <div class="section">
        <h2 class="section-title">Description</h2>
        <div class="description">
            {{ ressource.description }}
        </div>
    </div>

    {% if ressource.lien %}
    <div class="section">
        <h2 class="section-title">Resource Link</h2>
        <div>
            {{ ressource.lien }}
        </div>
    </div>
    {% endif %}

    <div class="meta-info">
        <p>Average Rating: {{ ressource.noteaverage|number_format(1) }}/10</p>
        <p>Generated on: {{ "now"|date("Y-m-d H:i:s") }}</p>
    </div>

    <div class="footer">
        <p>This document was generated from Onboardify - Resource Management System</p>
    </div>
</body>
</html> 