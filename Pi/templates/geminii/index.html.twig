{% extends 'base.html.twig' %}

{% block title %}Gemini AI - Chatbot{% endblock %}

{% block body %}
    <div class="container mt-5">
        <div class="card shadow-lg border-0">
            <div class="card-header bg-primary text-white text-center">
                <h1 class="mb-0">💬 Gemini AI Chatbot</h1>
            </div>
            <div class="card-body">
                <form method="post" action="{{ path('gemini_test') }}" class="needs-validation" novalidate>
                    <div class="mb-4">
                        <label for="prompt" class="form-label fw-bold">Saisissez votre question :</label>
                        <input type="text" class="form-control form-control-lg" id="prompt" name="prompt" placeholder="Exemple : Bonjour, comment vas-tu ?" required>
                        <div class="invalid-feedback">
                            Veuillez entrer une question valide.
                        </div>
                    </div>
                    <div class="d-grid">
                        <button type="submit" class="btn btn-primary btn-lg shadow">Générer une réponse</button>
                    </div>
                </form>

                {% if response is defined %}
                    <div class="alert alert-secondary mt-4" role="alert">
                        <h4 class="alert-heading">Réponse :</h4>
                        <p class="mb-0">{{ response }}</p>
                    </div>
                {% endif %}
            </div>
        </div>
    </div>

    <style>
        body {
            background: #f9fbfd;
            font-family: 'Nunito', sans-serif;
        }
        .form-control:focus {
            box-shadow: 0 0 10px rgba(33, 150, 243, 0.5);
            border-color: #2196f3;
        }
        .card {
            border-radius: 10px;
        }
        .btn-primary {
            background: linear-gradient(90deg, #4facfe 0%, #00f2fe 100%);
        }
        .btn-primary:hover {
            background: linear-gradient(90deg, #00f2fe 0%, #4facfe 100%);
        }
    </style>
{% endblock %}